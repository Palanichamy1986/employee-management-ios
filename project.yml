name: EmployeeApp
options:
  minimumXcodeVersion: "15.0"
  deploymentTarget:
    iOS: "17.0"

targets:
  EmployeeApp:
    type: application
    platform: iOS
    sources:
      - path: .
    settings:
      PRODUCT_NAME: EmployeeApp
      BUNDLE_ID: com.yourcompany.employeeapp
      CODE_SIGN_IDENTITY: Apple Development
